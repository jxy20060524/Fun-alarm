package com.funalarm;

import javafx.application.Application;

/**
 * JavaFX 启动入口。IDE 直接运行此类时需配置 VM 参数，见 README。
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(FunAlarmApp.class, args);
    }
}
