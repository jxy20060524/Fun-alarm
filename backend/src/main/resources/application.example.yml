# 复制本文件为 application-local.yml 并填写密码（application-local.yml 已在 .gitignore 中，不会提交）
spring:
  datasource:
    url: jdbc:mysql://bj-cynosdbmysql-grp-7t95af7a.sql.tencentcdb.com:20873/fun_alarm?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 你的密码
